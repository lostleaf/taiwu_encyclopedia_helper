# AGENTS.md

本文件为 Codex / Claude Code 等 agent 在本仓库中工作时的统一说明，请优先遵守这里的约束。

## 项目概述

本仓库是游戏《太吾绘卷》（The Scroll of Taiwu）游戏内「百晓册」Markdown 导出的本地知识库说明。

原始导出内容统一存放在本地 `EncyclopediaExporter_Output/`，但该目录被 `.gitignore` 排除，不进入公开仓库。仓库用途是作为只读知识库，供 agent 查询、阅读并回答关于游戏内容的问题。仓库中没有代码、构建脚本或测试，因此：

- 不需要执行 build、lint、test。
- 不要浪费时间寻找 `package.json`、`Makefile` 等工程文件。
- 主要工作是检索和阅读 Markdown 内容，并基于文件内容回答问题。
- 回答游戏机制、物品、门派、武学、数值等问题时，必须以仓库文件为依据，不要凭记忆编造。

## 只读内容约束

本地 `EncyclopediaExporter_Output/` 中的游戏内容是唯一权威来源。以下目录和文件中的游戏数据禁止由 agent 创建、修改、删除、重命名或提交到公开分支：

- `EncyclopediaExporter_Output/` 下的 12 个章节目录：`主页`、`启程`、`世界`、`门派`、`人物`、`交互`、`修习`、`战斗`、`产业`、`物品`、`游历`、`扩展`
- `EncyclopediaExporter_Output/词条/` 下的全部 `.md` 文件
- `EncyclopediaExporter_Output/章节大纲.md`

这些内容只能由用户手动更新，例如在游戏版本更新后重新导出。

agent 的职责是检索、阅读、总结和据实回答。若发现内容疑似有误、缺失或与游戏实机不一致，应在回答中说明并请用户核实，绝不可自行改动游戏内容文件。

仓库根目录的元数据、工具说明文件与本地派生笔记不受上述只读限制，例如 `CLAUDE.md`、`AGENTS.md`、`README.md`、`研究笔记/`、`.claude/` 等。但 `研究笔记/` 默认不进入公开 `main`。

## 内容结构

仓库内容分为三类：章节页、词条、派生研究笔记。

### 章节页

章节页位于 `EncyclopediaExporter_Output/` 下的 12 个章节目录中，用于解释游戏机制与概念：

`主页`、`启程`、`世界`、`门派`、`人物`、`交互`、`修习`、`战斗`、`产业`、`物品`、`游历`、`扩展`

命名通常遵循：

```text
EncyclopediaExporter_Output/{章节}/{子章节}/{条目}/{章节}-{子章节}-{条目}.md
```

章节页开头通常包含类似引用块：

```markdown
> 百科 Key: `战斗-战斗基础-施展功法`
> 层级: 战斗 / 战斗基础 / 施展功法
```

章节页之间使用相对路径互相链接，也会链接到下层词条。解析链接时，要以源文件所在目录为基准。

### 词条

词条全部位于 `EncyclopediaExporter_Output/词条/` 下，并按类别分子目录。每个文件对应一个具体游戏物品、功法、特性、属性或其他数据条目。

注意：

- 文件名通常就是条目名。
- 词条本身一般不包含跨条目链接。
- 类型、品级、门派、属性等信息在正文中，不一定体现在路径里。
- 例如功法均位于 `EncyclopediaExporter_Output/词条/功法/` 下，内功、身法、摧破、护体、奇窍等分类需读取正文判断。

### 派生研究笔记

`研究笔记/` 用于存放 agent 基于原始百科内容整理出的总结、对比表和检索结论。

注意：

- 研究笔记不是 ground truth，只能作为索引和摘要。
- 回答具体游戏问题时，应优先读取并引用 `EncyclopediaExporter_Output/` 中的原始文件。
- 若研究笔记与原始导出冲突，以原始导出为准，并在回答中说明差异。

## 检索建议

优先使用 `rg` 和 `rg --files`。

查具体条目时：

- 如果知道类别，直接定位文件，例如 `EncyclopediaExporter_Output/词条/功法/易筋经.md`。
- 如果不确定类别，用 `rg --files EncyclopediaExporter_Output/词条 | rg '易筋经\\.md$'`。

查机制或概念时：

- 优先阅读 `EncyclopediaExporter_Output/章节大纲.md`，它列出了章节页入口。
- 根据大纲定位对应章节页。
- 读取章节页后，顺着相对链接继续深入。
- 也可以从 `EncyclopediaExporter_Output/主页/主页/主页/主页-主页-主页.md` 的内容概览开始。

做模糊检索时：

- 使用 `rg '关键词' EncyclopediaExporter_Output` 搜索正文。
- 对中文关键词保持原文搜索，必要时尝试同义词或更短的词。
- 查跨条目关系时，结合类别目录和正文关键词，例如在 `EncyclopediaExporter_Output/词条/功法/` 中搜索门派名。

查本仓库已有整理时：

- 可以先检索 `研究笔记/` 获取线索。
- 线索涉及具体数值、机制或结论时，仍需回到 `EncyclopediaExporter_Output/` 原始文件核对。

## 回答要求

- 回答必须基于实际读取到的文件内容。
- 涉及具体数值时，提醒用户许多词条标有「具体数值以游戏内为准」。
- 若文件内容没有直接答案，应说明未在当前资料中找到明确依据。
- 若结论来自排除法或跨文件推断，应明确标为推断。
- 涉及剧情解锁、相枢、五方神龙等内容时，注意这些资料可能包含轻微剧透。
- 区分章节页与词条：开头有 `> 百科 Key:` 的通常是章节页；没有该引用块的通常是词条。
- 区分原始导出与派生笔记：`EncyclopediaExporter_Output/` 是 ground truth，`研究笔记/` 是总结。

## 工作边界

本仓库不是软件项目。除用户明确要求维护说明文件或派生笔记外，不要主动改动仓库内容。

可做的工作包括：

- 检索 Markdown 文件。
- 阅读并总结游戏机制。
- 对多个词条进行对比。
- 根据现有资料回答问答。
- 维护根目录下的 agent 说明文件。
- 在 `研究笔记/` 中维护派生总结。

不可做的工作包括：

- 修改 `EncyclopediaExporter_Output/` 中的游戏数据目录或词条文件。
- 自行补写、修正或重命名原始词条。
- 在没有用户要求时添加构建、测试、脚本或项目工程结构。

## Git 提交规范

- 公开分支重建时，第一个提交应为空提交，用于建立干净的公开历史起点。
- 后续提交信息使用 Conventional Commits 风格前缀，例如 `docs:`。
- 提交说明使用中文书面语，简洁描述本次变更。
