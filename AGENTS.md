# AGENTS.md

本文件是 Codex / Claude Code 等 agent 在本仓库工作的统一说明。优先遵守这里的约束。

## 项目定位

本仓库是游戏《太吾绘卷》（The Scroll of Taiwu）游戏内「百晓册」Markdown 导出的本地知识库。

- 本仓库不是软件项目，没有构建、lint、测试流程；不要寻找或新增 `package.json`、`Makefile` 等工程结构。
- 主要工作是检索、阅读、总结 Markdown，并基于仓库文件回答游戏机制、物品、门派、武学、数值等问题。
- 不要凭记忆编造游戏内容；回答必须以实际读取到的文件为依据。

## 数据来源与写入边界

`EncyclopediaExporter_Output/` 是游戏内容的唯一权威来源，但被 `.gitignore` 排除，不进入公开仓库。该目录应保持为 exporter mod 生成结果的原样拷贝。

禁止 agent 创建、修改、删除、重命名或提交以下原始游戏数据：

- `EncyclopediaExporter_Output/` 下的章节目录：`主页`、`启程`、`世界`、`门派`、`人物`、`交互`、`修习`、`战斗`、`产业`、`物品`、`游历`、`扩展`
- `EncyclopediaExporter_Output/词条/` 下的全部 `.md` 文件

如果发现原始内容疑似有误、缺失或与游戏实机不一致，只能在回答中说明并请用户核实；不要自行修正原始导出。

允许维护仓库元数据、agent 说明和派生笔记，例如 `AGENTS.md`、`CLAUDE.md`、`README.md`、`.claude/`、`研究笔记/`。派生索引、摘要和研究结论应放在 `研究笔记/` 等派生目录，绝不能写入 `EncyclopediaExporter_Output/`。`研究笔记/` 默认不进入公开 `main`。

## 内容结构

仓库内容分为章节页、词条和派生研究笔记。

### 章节页

章节页位于 `EncyclopediaExporter_Output/` 下的 12 个章节目录中，用于解释游戏机制与概念。

常见路径格式：

```text
EncyclopediaExporter_Output/{章节}/{子章节}/{条目}/{章节}-{子章节}-{条目}.md
```

章节页开头通常包含：

```markdown
> 百科 Key: `战斗-战斗基础-施展功法`
> 层级: 战斗 / 战斗基础 / 施展功法
```

章节页之间使用相对路径互相链接，也会链接到下层词条。解析链接时，以源文件所在目录为基准。

### 词条

词条全部位于 `EncyclopediaExporter_Output/词条/` 下，并按类别分子目录。每个文件对应一个具体游戏物品、功法、特性、属性或其他数据条目。

- 文件名通常就是条目名。
- 词条本身一般不包含跨条目链接。
- 类型、品级、门派、属性等信息在正文中，不一定体现在路径里。
- 例如功法均位于 `EncyclopediaExporter_Output/词条/功法/` 下，内功、身法、摧破、护体、奇窍等分类需读取正文判断。

### 研究笔记

`研究笔记/` 是 agent 基于原始百科整理出的派生内容，可作为索引和摘要，但不是 ground truth。

- 回答具体游戏问题时，优先读取 `EncyclopediaExporter_Output/` 原始文件。
- 若研究笔记与原始导出冲突，以原始导出为准，并在回答中说明差异。
- `研究笔记/章节大纲.md` 如存在，是派生索引，不是 exporter mod 自带文件。

## 检索方法

优先使用 `rg` 和 `rg --files`。

查具体条目：

- 已知类别时，直接定位文件，例如 `EncyclopediaExporter_Output/词条/功法/易筋经.md`。
- 不确定类别时，用 `rg --files EncyclopediaExporter_Output/词条 | rg '易筋经\.md$'`。

查机制或概念：

- 可先检索 `研究笔记/章节大纲.md` 获取入口，但具体结论仍需回到原始文件核对。
- 也可从 `EncyclopediaExporter_Output/主页/主页/主页/主页-主页-主页.md` 的内容概览开始。
- 定位章节页后，顺着相对链接继续深入。

做模糊或跨条目检索：

- 用 `rg '关键词' EncyclopediaExporter_Output` 搜索正文。
- 中文关键词保持原文搜索，必要时尝试同义词或更短关键词。
- 查跨条目关系时，结合类别目录和正文关键词，例如在 `EncyclopediaExporter_Output/词条/功法/` 中搜索门派名。

## 回答要求

- 回答必须基于实际读取到的文件内容。
- 若文件内容没有直接答案，说明未在当前资料中找到明确依据。
- 若结论来自排除法或跨文件推断，明确标为推断。
- 涉及具体数值时，提醒用户许多词条标有「具体数值以游戏内为准」。
- 涉及剧情解锁、相枢、五方神龙等内容时，注意这些资料可能包含轻微剧透。
- 区分章节页、词条和派生笔记：有 `> 百科 Key:` 的通常是章节页；`EncyclopediaExporter_Output/` 是 ground truth；`研究笔记/` 只是总结。

## 品级常识

品级代表物品、功法、技艺等的稀有程度，由低到高共九级：

| 品级 | 前缀 |
|------|------|
| 九品（最低） | 下 |
| 八品 | 中 |
| 七品 | 上 |
| 六品 | 奇 |
| 五品 | 秘 |
| 四品 | 极 |
| 三品 | 超 |
| 二品 | 绝 |
| 一品（最高） | 神 |

九品、八品、七品统称下三品；六品、五品、四品统称中三品；三品、二品、一品统称上三品。词条中品级标注格式为 `前缀·品级`，例如 `神·一品`、`绝·二品`、`下·九品`。

## Git 提交规范

- 公开分支重建时，第一个提交应为空提交，用于建立干净的公开历史起点。
- 后续提交信息使用 Conventional Commits 风格前缀，例如 `docs:`。
- 提交说明使用中文书面语，简洁描述本次变更。
